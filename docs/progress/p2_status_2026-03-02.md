---
title: P2 执行状态（2026-03-02 起，补记至 2026-03-17）
---

# P2 执行状态（2026-03-02 起，补记至 2026-03-17）

> 范围：`flutter_rust_net` 的 P2（缓存体系补齐）首轮启动状态。

## 快速跳转（同日文档）

- 路线优先级：[`flutter_rust_net/FLUTTER_RUST_NET_PRIORITY_ROADMAP_ZH.md`](../../FLUTTER_RUST_NET_PRIORITY_ROADMAP_ZH.md)
- P1 执行状态：[`flutter_rust_net/docs/progress/p1_status_2026-02-25.md`](./p1_status_2026-02-25.md)
- 测试运行记录：`相关文档（按需）`

## 文档口径（单一事实源）

- 本文是 **P2 执行进度** 的唯一事实源（Done / In Progress / Next 仅在此更新）。
- 具体测试编号与原始结果以 `相关文档（按需）` 为准；本文只做阶段汇总。
- `相关文档（按需）` 按需要同步更新即可。
- 若与路线图/P1 文档出现冲突，以本文（按日期）为准，并同步修订其他文档。

## 更新流程（建议）

1. 如需补充测试细节，可按需更新 `相关文档（按需）`。
2. 再更新本文 `Done / In Progress / Next`，并补记日期与结论变化。
3. 若阶段优先级变化，同步更新 `flutter_rust_net/FLUTTER_RUST_NET_PRIORITY_ROADMAP_ZH.md`。
4. 最后自检“快速跳转”与“单一事实源”口径是否一致。

## 1) 已完成（Done）

1. 已将 Rust `DiskCache` 从占位实现升级为可用基础实现（GET 场景）。
2. 已落地缓存 key 归一化：`method + normalized_url + headers + body_hash + version`。
3. 已补齐 `TTL + ETag/Last-Modified` 条件重验证链路（`If-None-Match` / `If-Modified-Since`）。
4. 已在 `NetEngine::request` 接入缓存读取/回源/回写流程，命中新鲜缓存时返回 `from_cache=true`。
5. 已补齐命名空间治理与清理入口复用（`clear_cache(namespace)` 统一走 `DiskCache`）。
6. 已补齐 `TTL + LRU` 清理策略（默认 namespace 容量预算 64MB）。
7. 已新增 Rust 单测覆盖缓存核心行为（命中、重验证、LRU、namespace 清理与安全校验）。
8. 已完成缓存策略参数外置最小闭环：新增 `cache_default_ttl_seconds` / `cache_max_namespace_bytes`（Rust 配置 + 默认值兜底）。
9. 已打通 Dart 侧配置入口：`RustEngineInitOptions -> rust_api.NetEngineConfig -> NetEngine -> DiskCache`。
10. 已新增 Rust 单测覆盖零 TTL 行为：当 `DiskCache` 默认 TTL 为 `0` 且响应无缓存头时，缓存条目不会被当作新鲜命中；当前对外初始化配置链路中的 `cache_default_ttl_seconds=0` 仍表示“使用默认值兜底”。
11. 已完成 `engine/cache.rs -> engine/cache/` 模块化拆分，公共 API 与缓存行为保持不变。
12. 已补齐跨通道缓存契约回归（缓存关闭/开启）：新增 `test/network/cache_channel_consistency_test.dart`，以 harness 方式覆盖 `no-store` 与 `max-age` 口径下的 Dio/Rust 响应契约对齐，并已在后续补齐真实 Rust FFI 链路复测。
13. 已补齐缓存命中可观测性对齐：新增 `NetResponse.fromCache`，并将 Rust `ResponseMeta.fromCache` 透传到 Dart 统一响应模型。
14. 已新增映射回归：`test/network/rust_adapter/rust_adapter_request_test.dart` 增补 `fromCache` 字段透传断言，避免后续桥接回归。
15. 已将缓存指标接入 benchmark 报告输出：新增 `cacheHit/cacheMiss/repeatedMissCount/cacheRevalidate/cacheEvict` 字段，并补充 `--request-key-space` 复用 key 能力用于缓存收益观测；当前客户端侧 repeated miss 单独记到 `repeatedMissCount`，`cacheRevalidate/cacheEvict` 仅在本地 scenario server 口径下承载条件请求计数与重复回源信号。
16. 已对齐“缓存副本”与“响应 materialize 文件”的默认生命周期边界：Rust 普通请求的默认落盘文件已从 `cache_dir` 根目录迁移到独立 sibling 目录，`clear_cache` 清理缓存副本时不再误删待 Dart 消费的响应文件；已新增 Rust 回归覆盖“网络命中 -> 缓存命中 -> clear_cache”链路。
17. 已补跑公网 `jitter_latency` 缓存收益本地执行产物摘要（`build/remote_cache_probe_20260313/remote_jitter_cache_{cold,warm}.json`）：`cold-start` 下 Rust `cacheHit=82/96`、`reqP95=85ms`、`throughput=405 req/s`，优于 Dio `cacheHit=0/96`、`reqP95=120ms`、`throughput=186 req/s`；`warm-cache` 下 Rust `cacheHit=96/96`、`reqP95=7ms`、`throughput=1371 req/s`，显著优于 Dio `reqP95=43ms`、`throughput=226 req/s`。
18. 已确认上述历史 external 样例窗口内命中的都是 fresh TTL 路径；当前实现下若要观察 `cacheRevalidate/cacheEvict` 权威信号，应优先使用本地 scenario server 口径。
19. 已修正 `clear_cache` 的空白 namespace 风险：`clear_cache("   ")` 不再退化为“全量清理”，并已补齐 Rust/Dart 回归覆盖空白 namespace、trim 后 targeted clear 与 `cacheMaxNamespaceBytes` 配置漂移拒绝。
20. 已补齐 `DiskCache` 级 namespace 容量预算互不干扰回归：新增多 namespace Rust 单测，确认 LRU 淘汰仅在各自 namespace 内生效，不会把容量预算误实现为全局共享上限。
21. 已补齐真实 Rust FFI 的 cache on/off 一致性复测：新增 `test/network/rust_adapter_real_bridge_test.dart` 覆盖 `no-store` 与 `max-age` 场景，确认真实桥接链路下 Rust `fromCache` 口径与请求命中行为符合预期。
22. 已将 benchmark 缓存指标口径同步到远端/真机归档模板：更新真机命令清单与 benchmark runbook，统一要求归档 `cacheHit/cacheMiss/repeatedMissCount`，并明确 external `baseUrl` 下 `cacheRevalidate/cacheEvict` 不作为权威字段。
23. 已补齐默认响应缓存 namespace 外置：新增 `cache_response_namespace` / `cacheResponseNamespace` 初始化配置，Rust 请求缓存读写不再硬编码 `responses`，并已补齐非法 namespace 拒绝与共享初始化配置冲突回归。
24. `cacheResponseNamespace` review 已收口：Dart 已同步 Rust 的 trailing-dot namespace 拒绝规则与非空 `cacheDir` 的 `trim()` 语义；对应问题单现为 `Ready / resolved`，相关 Dart 定向回归已覆盖这两条残留口径问题。
25. 已补齐 external `jitter_latency` + root budget 首批 cold / warm 归档样例（`build/remote_cache_root_budget_20260317_143738/remote_jitter_root_budget_{cold,warm}.json`）：两份 JSON 均稳定产出 `config.rustCache*` 与 `rustCacheObservation.rootBytes=7660`；同一 `cacheRootMaxBytes=25165824` 下，cold / warm 的 rootBytes 保持一致且显著低于预算上限。

## 1.1) 会话补记（2026-03-03）

1. P2 从“能力落地”进入“收益观测与跨通道一致性回归”阶段，基础缓存能力已具备继续扩展条件。
2. 代码层已完成“参数外置 + Dart 入口透传 + Rust 行为回归”的最小闭环，可支撑后续 namespace 策略扩展。
3. 与 P1 并行关系保持不变：P2 持续推进缓存收益，P1 仍负责 upload 一致性与准入证据收尾。
4. Next-1（缓存开关下 Dio vs Rust 一致性）已完成契约级首轮回归闭环，后续聚焦真实 Rust FFI 链路补跑与指标观测。

## 1.2) 会话补记（2026-03-05）

1. benchmark 结果已具备缓存维度基础口径：客户端侧 `hit/miss` + 本地场景服务侧 `conditional/repeated-origin` 信号（输出字段名仍为 `revalidate/evict`）。
2. `--request-key-space` 已可用于构造重复请求键，支持在同一压测命令下观测缓存收益变化。
3. 后续重点从“字段接入”转为“真实链路补跑与阈值沉淀”。

## 1.3) 会话补记（2026-03-13）

1. P2 当前第一优先项“缓存副本 vs 响应 materialize 文件”边界已完成首轮闭环，默认临时响应文件不再与 `DiskCache` 共享同一目录根。
2. 当前 `clear_cache` 的职责已收敛为“清理缓存副本”，不再承担待 materialize 响应文件的兜底删除；响应文件仍由 Dart 在 materialize 后 best-effort 清理。
3. 默认 `save_to_file_path` 仍视为调用方显式拥有的输出路径，本轮隔离仅覆盖 Rust 内部默认 materialize 路径。

## 1.4) 会话补记（2026-03-13，真实链路缓存收益与 namespace 回归）

1. 已在公网 `jitter_latency` 场景完成 P2 首轮缓存收益补跑，样例记录见 `docs/dio_rust_test/network_public_remote_cache_probe_2026-03-13.md`。
2. 当前 external `baseUrl` 口径下，客户端 repeated miss 已改由 `repeatedMissCount` 表示；`cacheEvict` 不再混入 external 近似值。
3. namespace 清理口径已补一道安全闸：显式传入空白 namespace 会报错，不再误判为 `None -> clear all`。
4. `cacheMaxNamespaceBytes` 相关共享初始化漂移已纳入 Dart 回归，避免 namespace 预算配置静默沿用旧值。

## 1.5) 会话补记（2026-03-14，namespace 预算隔离回归）

1. 已新增 `DiskCache` 多 namespace 容量预算隔离单测，确认单 namespace 容量上限只约束各自分区，不会跨 namespace 互相驱逐缓存条目。
2. 在该回归下，缓存根目录总占用允许高于单 namespace budget；当前预算语义明确为“每个 namespace 独立上限”，不是全局 cache root 总预算。

## 1.6) 会话补记（2026-03-14，真实 Rust FFI 缓存一致性复测）

1. 已在真实 Rust bridge 测试文件中补齐 cache-off 与 cache-on 两个场景，覆盖 `Cache-Control: no-store` 请求禁用缓存与 `Cache-Control: max-age=120` 响应命中缓存两条路径。
2. 当前真实链路回归已确认：cache-off 下 Rust 与 Dio 都会继续回源；cache-on 下 Rust 第二次请求可直接返回 `fromCache=true`，而 Dio 仍保持两次回源。

## 1.7) 会话补记（2026-03-14，缓存指标归档模板同步）

1. 已将 P2 缓存收益字段同步到真机命令清单与 benchmark runbook 的归档模板，后续留档至少要求 `cacheHit/cacheMiss/repeatedMissCount + reqP95/throughput`。
2. external `baseUrl` 路径下的 `cacheRevalidate/cacheEvict` 已在模板中明确为“非权威字段”；若需要权威条件请求/重复回源统计，应改用本地 scenario server 口径。

## 1.8) 会话补记（2026-03-14，默认 namespace 外置）

1. 已将 Rust 请求缓存默认 namespace 从实现常量提升为初始化配置：Dart `RustEngineInitOptions` 与 Rust `NetEngineConfig` 新增 `cacheResponseNamespace/cache_response_namespace`，默认值仍为 `responses`。
2. 当前实现已确认：同一 engine scope 下若请求方传入不同的响应缓存 namespace，会被视为配置漂移并拒绝复用，避免共享 Rust engine 在不同分区语义之间静默串用。
3. Rust 侧初始化已拒绝空白或非法 response cache namespace；新增 Rust 回归确认自定义 namespace 会实际落到目标目录而不是默认 `responses` 分区。

## 1.9) 会话补记（2026-03-14，cacheResponseNamespace review 首轮修复）

1. Rust `normalize_namespace()` 已收紧为单层 segment 语义，`./responses`、`tenant/a`、`tenant\\a` 等路径语义输入现会被拒绝，避免 namespace 落成多级目录或目录别名。
2. Dart `_RustAdapterInitTracker.toNetEngineConfig()` 已在进入 FRB `NetEngineConfig` 前规范化 `cacheResponseNamespace`，共享 initialized scope 不再把 trim 前后等价值误判为配置冲突。
3. Rust 兼容性口径已回退到更合理边界：只有在 `cache_dir` 非空、真正启用磁盘缓存时，Rust 才会关心 `cacheResponseNamespace` 是否有效；Dart 侧也已在“缓存关闭 + 非法 namespace”主路径上回落到默认 `responses`。
4. 本轮已完成定向验证：`cargo test -q normalize_namespace_rejects_path_like_inputs`、`cargo test -q net_engine_rejects_invalid_configured_response_cache_namespace`、`cargo test -q net_engine_allows_invalid_response_cache_namespace_when_cache_disabled`、`cargo test -q request_cache_uses_configured_response_namespace`、`flutter test test/network/rust_adapter/rust_adapter_initialization_test.dart`、`flutter test test/network/rust_adapter/rust_adapter_request_test.dart`。
5. 严格复核后确认本项尚未完全收口：当前仍有 2 个残留问题待补齐，分别是 Windows 目录别名输入 `responses.` 未被拒绝，以及 Dart 对空白 `cacheDir` 的共享初始化比较仍可能把 Rust 等价值误判为冲突。

## 1.10) 会话补记（2026-03-14，cacheResponseNamespace review 收尾完成；后续已由 1.12 回退）

1. Rust `normalize_namespace()` 已新增 trailing-dot 拒绝规则，`responses.`、`tenant_cache..` 等 Windows 目录别名输入现会在统一入口被拒绝。
2. Dart `_RustAdapterInitTracker.toNetEngineConfig()` 已新增 `_normalizeCacheDir()`；`cacheDir: ''` 与 `cacheDir: '   '` 现统一规范化为 `''`，共享 initialized scope 与 Rust 的“缓存关闭”语义完全对齐。
3. 本轮已完成定向验证：`cargo test -q normalize_namespace_rejects_path_like_inputs`、`flutter test test/network/rust_adapter/rust_adapter_initialization_test.dart`、`flutter test test/network/rust_adapter/rust_adapter_request_test.dart`。
4. `cacheResponseNamespace` review 中记录的 2 个残留问题已收口，对应问题单可关闭；后续只保留 namespace 级预算治理与真机样例补证。
5. 补充说明：上述“收尾完成”判断已被下文 `1.12` 的暂存区严格复核结果回退；本节仅保留为当时记录。

## 1.11) 会话补记（2026-03-14，namespace 预算治理方案整理）

1. 已新增 `docs/plan/cache_namespace_budget_governance_plan_2026-03-14.md`，把当前预算语义、扩展触发条件和推荐路线整理成单独方案文档。
2. 当前结论明确为：`cacheMaxNamespaceBytes` 继续视为“每 namespace 独立上限”的正式契约，不立即扩展为显式分区策略。
3. 若后续要治理同一 cache root 下多 namespace 的总盘占用，优先考虑新增可选 `cacheRootMaxBytes`，而不是先引入复杂的分区权重表。

## 1.12) 会话补记（2026-03-14，暂存区严格复核补记）

1. 严格复核暂存区后确认：`cacheResponseNamespace` review 还不能关闭。虽然 Rust 已拒绝 trailing-dot namespace，Dart `_normalizeCacheResponseNamespace()` 仍未同步该规则，当前仍会出现“Dart 放行、Rust init 再失败”的校验分叉。
2. Dart `_normalizeCacheDir()` 当前只把空白字符串规范化到 `''`；但 Rust 在真正使用 `cache_dir` 前会统一 `trim()`，因此非空路径前后空白仍可能被 Dart 误判为配置漂移。
3. 问题单与 P2 进度文档此前都已提前写成“收尾完成”；本轮已回退结论，避免误导后续提交判断。
4. 后续需要先补齐上述两条 Dart/Rust 规范化残留问题及回归，再重新关闭 `cacheResponseNamespace` review。

## 1.13) 会话补记（2026-03-16，root budget 收口闭环）

1. 已新增可选 `cacheRootMaxBytes / cache_root_max_bytes` 配置，并打通 Dart `RustEngineInitOptions`、FRB bridge、Rust `NetEngineConfig` 与 `DiskCache`。
2. root budget 当前按 cache root 实际文件占用生效：`DiskCache` 会计入 body + metadata，并在 root 扫描时清理残留文件；预算命中时仍按跨 namespace 的最近访问时间做全局淘汰。
3. `NetEngine` 初始化 cache 时会先执行一次 root prune；若用户在已有缓存目录上首次开启 `cacheRootMaxBytes`，不需要等待后续写入才能收敛。
4. `_RustAdapterInitTracker` 已把 `cacheRootMaxBytes` 纳入共享初始化一致性比较；缓存关闭时会统一归一化为 `null`，避免无效配置漂移；同时已补齐 Dart `u32` 上界校验，避免把非法超大值泄漏成 FRB 序列化异常。
5. 已补齐回归：
   - Rust：跨 namespace root budget 淘汰、metadata 实际占用计量、残留文件清理、初始化阶段已有超限目录收敛、root prune 与 metadata 临时文件写入的并发竞争保护
   - Dart：共享初始化预算漂移拒绝、缓存关闭时 root budget 忽略、init 配置透传、`cacheRootMaxBytes > u32::MAX` 预检拒绝
6. root budget 的正式契约已补充明确：启用 `cacheRootMaxBytes` 时，`cacheDir` 必须是当前组件独占的专用目录，不能指向共享目录；否则 root prune 会清理不属于当前 cache 布局的文件。
7. 本轮补充验证已通过：
   - `cargo test -q lookup_waits_out_concurrent_root_prune_during_metadata_temp_write --manifest-path native/rust/net_engine/Cargo.toml`
   - `cargo test -q root_prune_removes_residual_files_under_cache_root --manifest-path native/rust/net_engine/Cargo.toml`
   - `cargo test -q stores_and_reads_fresh_cache_entry --manifest-path native/rust/net_engine/Cargo.toml`
   - `cargo test -q stale_entry_with_validator_can_be_revalidated --manifest-path native/rust/net_engine/Cargo.toml`
   - `cargo test -q root_byte_budget_prunes_across_namespaces --manifest-path native/rust/net_engine/Cargo.toml`

## 1.14) 会话补记（2026-03-16，benchmark root budget 观测接线）

1. benchmark 侧已新增 Rust 缓存配置接线：`BenchmarkConfig` / `tool/network_bench.dart` 现支持 `rustCacheDir`、`rustCacheResponseNamespace`、`rustCacheMaxNamespaceBytes`、`rustCacheRootMaxBytes`，并统一映射到 `RustEngineInitOptions`。
2. benchmark 报告已新增 `rustCacheObservation`：当 Rust 实际初始化且磁盘缓存启用时，JSON 与 pretty text 会记录有效 `cacheDir` 与 cache root 实际文件占用 `rootBytes`。
3. 这意味着“补 root budget 口径与占用观测”这一步在 benchmark 代码层已完成；当前剩余工作转为实际跑 Android / iOS / external 归档样例，并验证 `rootBytes` 与 `cacheRootMaxBytes` 是否能稳定解释行为。
4. 本轮针对性验证已通过：
   - `flutter test test/network/benchmark_types_test.dart test/network/network_realistic_flow_test.dart`
   - `dart run tool/network_bench.dart --help`

## 1.15) 会话补记（2026-03-16，benchmark root budget review 问题收口）

1. benchmark 运行前现会先生成“本次实际生效配置”：若请求初始化 Rust 且未显式传 `rustCacheDir`，benchmark 会自动分配本次 run 独占的 cache root，不再复用共享默认目录。
2. `BenchmarkReport.config` 现记录实际用于 Rust init 的归一化后缓存配置；`cacheDir` trim、`cacheResponseNamespace` trim、`cacheMaxNamespaceBytes=0 -> 默认值`、`cacheRootMaxBytes=0 -> null`、以及“缓存关闭时忽略 namespace/root budget”这些语义，已与 Dart/Rust 初始化口径对齐。
3. 这意味着 `rustCacheObservation.rootBytes` 与报告中的 `config.rustCache*` 字段可以一起解释同一次 benchmark run 的实际 cache root 行为；cold / warm 需要复用同一 cache root 时，仍应显式传入相同的 `--rust-cache-dir`。
4. 本轮补充验证已通过：
   - `flutter test test/network/benchmark_types_test.dart test/network/network_realistic_flow_test.dart test/network/rust_adapter/rust_adapter_initialization_test.dart`
   - `dart run tool/network_bench.dart --help`

## 1.16) 会话补记（2026-03-17，benchmark auto cache root follow-up 收口）

1. `RustAdapter` 现会记录当前 adapter 是否真正创建了活动 generation；`runNetworkBenchmark()` 只在本次 benchmark 自己创建 Rust scope 时执行 `shutdownEngine()`，不会越权关闭外部已持有的共享 scope。
2. 若本次 benchmark 省略 `rustCacheDir` 走 auto cache root，runner 现会在采集 `rustCacheObservation.rootBytes` 后把该目录按 benchmark-owned 临时目录 best-effort 删除；显式传入的 cache root 继续由调用方治理。
3. 已新增 `test/network/benchmark_runner_test.dart`，覆盖同进程双次 Rust benchmark、auto cache root 回收，以及“复用外部已初始化 scope 时 runner 不越权 shutdown”这三条回归路径。
4. 本轮补充验证已通过：
   - `flutter test test/network/benchmark_runner_test.dart test/network/benchmark_types_test.dart test/network/network_realistic_flow_test.dart test/network/rust_adapter/rust_adapter_initialization_test.dart`
   - `dart run tool/network_bench.dart --help`

## 1.17) 会话补记（2026-03-17，external root budget 样例归档）

1. 已按 runbook 补跑一组 external `jitter_latency` cold / warm root budget 样例，显式复用同一个 `rust-cache-dir`；样例记录见 `docs/dio_rust_test/network_public_remote_root_budget_probe_2026-03-17.md`。
2. 两份 JSON 的 `config` 均稳定记录了 `rustCacheDir`、`rustCacheResponseNamespace`、`rustCacheMaxNamespaceBytes`、`rustCacheRootMaxBytes`，且 `rustCacheObservation.rootBytes` 均为 `7660`。
3. 本轮样例中 `rootBytes=7660` 与 cache root 实际递归文件大小一致，并始终满足 `rootBytes <= cacheRootMaxBytes (25165824)`；对 external 首批样例而言，当前 benchmark 观测字段已足够解释 cache root 收敛行为。
4. P2 的 external 首批 root budget 样例已收口，下一步重点回到 Android / iOS 真机同口径归档。

## 1.18) 会话补记（2026-03-17，cacheResponseNamespace 状态回写）

1. 此前 `1.12` 记录的两个 Dart 残留口径问题，后续已按问题单收口：`_normalizeCacheResponseNamespace()` 已拒绝 trailing-dot namespace，`_normalizeCacheDir()` 也已与 Rust 对非空 `cacheDir` 的 `trim()` 语义对齐。
2. 对应提交判断文档 `docs/problems/archive/cache_response_namespace_review_issues_2026-03-14.md` 当前状态为 `resolved` / `Ready`，且已记录两条 Dart 定向回归通过。
3. 本轮已把本文顶部 Done 项同步回写为“已收口”，避免继续把已关闭的 `cacheResponseNamespace` review 误展示为当前阻塞。

## 2) 当前正在做（In Progress）

1. 继续补 Android / iOS 真机样例，并沿用已同步的缓存指标归档模板留档；若启用 root budget，同步记录 `cacheRootMaxBytes` 与 `rustCacheObservation.rootBytes`。
2. 继续在 Android / iOS 真机样例与后续 external 复测中观察 `rootBytes <= cacheRootMaxBytes` 是否长期成立，并确认现有 benchmark 观测字段仍足够解释 cache root 收敛行为。

## 3) 下一步准备做（Next）

1. 补 Android / iOS 真机样例，并沿用同一套 root budget 参数与归档模板，判断 `rootBytes <= cacheRootMaxBytes` 是否长期成立。
2. 若 Android / iOS 首批归档或后续 external 复测仍不足以解释行为，再考虑继续补更细的 root usage 指标；在此之前不再新增 benchmark 观测字段。
3. 仅在业务明确需要多档缓存配额时，再继续评估显式分区策略。
