---
title: P1 执行状态（2026-02-25 起，补记至 2026-03-03）
---

# P1 执行状态（2026-02-25 起，补记至 2026-03-03）

> 范围：`flutter_rust_net` 的 P1（性能与容量瓶颈）推进状态。

## 快速跳转（同日文档）

- 路线优先级：[`flutter_rust_net/FLUTTER_RUST_NET_PRIORITY_ROADMAP_ZH.md`](../../FLUTTER_RUST_NET_PRIORITY_ROADMAP_ZH.md)
- 架构与能力概览：[`flutter_rust_net/FLUTTER_RUST_NET_OVERVIEW_ZH.md`](../../FLUTTER_RUST_NET_OVERVIEW_ZH.md)
- 测试运行记录：[`docs/test_plans/test_run_log.md`](../../../docs/test_plans/test_run_log.md)

## 文档口径（单一事实源）

- 本文是 **P1 执行进度** 的唯一事实源（Done / In Progress / Next 仅在此更新）。
- 具体测试编号与原始结果以 `docs/test_plans/test_run_log.md` 为准；本文只做阶段汇总。
- 若与路线图/概览出现冲突，以本文（按日期）为准，并同步修订其他文档。

## 更新流程（建议）

1. 新增或补测完成后，先更新 `docs/test_plans/test_run_log.md`（保留测试编号与口径）。
2. 再更新本文的 `Done / In Progress / Next`，并写清日期与结论变化。
3. 若阶段优先级或里程碑变化，同步更新 `flutter_rust_net/FLUTTER_RUST_NET_PRIORITY_ROADMAP_ZH.md`。
4. 若架构边界、术语或能力定义变化，同步更新 `flutter_rust_net/FLUTTER_RUST_NET_OVERVIEW_ZH.md`。
5. 最后自检三份文档的“快速跳转”和“单一事实源”是否仍一致。

## 1) 已完成（Done）

1. 已确认 P1 目标与执行口径（并发分档、`maxInFlightTasks`、路由阈值）。
2. 已新增 P1 执行模板文档：`flutter_rust_net/docs/dio_rust_test/network_p1_execution_template_2026-02-25.md`。
3. 已新增聚合脚本：`flutter_rust_net/tool/p1_aggregate.dart`，可自动汇总 `jitter` 报告并输出 PASS/FAIL。
4. 已完成一次 smoke 验证（单组 `jitter`）并产出示例汇总：
   - `flutter_rust_net/build/p1_jitter/p1_summary_none.md`
   - `flutter_rust_net/build/p1_jitter/p1_summary_none.json`
5. 已完成 P1-L1 全量粗扫（`c8/c16/c32 x mif12/24/32/48 x 3轮`，共 36 份报告）：
   - `flutter_rust_net/build/p1_jitter/20260225_1440/`
6. 已完成本轮 L1 聚合并输出：
   - `flutter_rust_net/build/p1_jitter/20260225_1440/p1_summary_none.md`
   - `flutter_rust_net/build/p1_jitter/20260225_1440/p1_summary_none.json`
7. 已完成 P1-L2 复验（`json_model`，6 组 Top2 x 5 轮，共 30 份报告）：
   - `flutter_rust_net/build/p1_jitter/20260225_1448/`
8. 已完成本轮 L2 聚合并输出（全部 PASS）：
   - `flutter_rust_net/build/p1_jitter/20260225_1448/p1_summary_model.md`
   - `flutter_rust_net/build/p1_jitter/20260225_1448/p1_summary_model.json`
9. 已回填路由策略文档（新增 P1 复验更新段）：
   - `flutter_rust_net/docs/dio_rust_test/network_route_strategy_2026-02-24.md`
10. 已追加测试运行记录两条（L1/L2）：
    - `docs/test_plans/test_run_log.md`
11. 已产出 P1 聚合结论文档：
    - `flutter_rust_net/docs/dio_rust_test/network_benchmark_p1_aggregation_2026-02-25.md`
12. 已新增独立示例承载壳（不依赖 `media_kit_poc`）：
    - `flutter_rust_net/example/`
13. 已将 Android Rust `.so` 构建/打包脚本迁移到示例工程：
    - `flutter_rust_net/example/android/app/build.gradle.kts`
14. 已切换为测试模式单开关路由（Rust 主通道 + Dio 回切）：
    - `flutter_rust_net/lib/network/net_feature_flag.dart`
    - `flutter_rust_net/lib/network/routing_policy.dart`
15. 已清理旧灰度分流字段与策略（阈值/allowlist/denylist）并完成回归：
    - `flutter_rust_net/test/network/routing_policy_test.dart`
    - `flutter_rust_net/test/network/network_gateway_test.dart`
16. 已补充 Rust 主通道路由基线（small_json + jitter/mif32）并入库：
    - `flutter_rust_net/build/small_json_rust_first.json`
    - `flutter_rust_net/build/jitter_latency_rust_first_mif32.json`
    - `docs/test_plans/test_run_log.md`（`TR-20260225-14` / `TR-20260225-15`）
17. 已同步架构/策略相关文档到测试模式：
    - `flutter_rust_net/docs/flutter_rust_network_layer_design.md`
    - `flutter_rust_net/docs/dio_rust_test/network_route_strategy_2026-02-24.md`
    - `flutter_rust_net/docs/flutter_rust_network_layer_design_review_findings_2026-02-24.md`
    - `flutter_rust_net/FLUTTER_RUST_NET_OVERVIEW_ZH.md`

## 1.1) 会话补记（2026-02-26）

1. 已确认下一阶段验证口径：以真机网络剖面 + 远端链路为主，loopback 基准仅作为回归参考。
2. 已盘点当前可复用 benchmark JSON 资产（基线 + P1-L1/L2 原始报告 + 聚合报告）。
3. 已确认产物归档方向：真机测试生成的 JSON 需统一上传到 TOS，作为准入与追溯依据。
4. 当前待补齐的外部输入：TOS 类型（平台）、鉴权方式、目标 bucket/prefix。

## 1.2) 会话补记（2026-02-28）

1. 已完成一次 P1 复评：当前仍属于“业务 App 接入前”验证阶段，尚未形成最终准入结论。
2. 新识别阻塞项：Rust transfer 的 upload 语义尚未与 Dio 行为完全对齐，需补实现与一致性回归。
3. 新识别口径偏差：`maxInFlightTasks` 文档建议值为 `32`，但代码/CLI 仍存在默认 `12`，会导致 `jitter` 结果漂移。
4. 本地复验观察（loopback，仅作回归参考）：`jitter(c16)` 下 `mif=32` 可恢复 Rust 优势，`mif=12` 出现明显退化。
5. 工程一致性待确认：`write_timeout_ms`、`max_connections` 等配置项需补“是否实际生效”的验证与记录。
6. 真机弱网 / 远端链路 / TOS 归档仍未闭环，继续作为 P1 准入前置条件。
7. 归档链路外部输入有进展：已确认日志上传入口 `http://47.110.52.208:7777/upload`，待补齐鉴权与命名规范。
8. 已补齐上传动作入口：CLI 脚本 + 示例 App 按钮（真机可直接点击上传报告 JSON）。
9. 已确认登录入口：`POST /user/login`（当前示例 App 上传前先登录并附带 token）。
10. 上传请求头已按服务端口径补齐：`token: <actual-token>`（并保留 `Authorization` 兼容头）。

## 1.3) 会话补记（2026-03-02）

1. 真机测试已完成首轮验证，P1 当前从“准入证据收集”转入“收尾与回填”阶段。
2. 后续开发已开始并行启动 P2：先落地 Rust `DiskCache` 的可用基础能力（GET + TTL/ETag/LRU）。
3. P2 启动进度已单独记录到：`flutter_rust_net/docs/progress/p2_status_2026-03-02.md`。
4. 真机首轮 smoke 结果已回填 `TR-20260302-36`：3 份 JSON 全部 `exceptions/http5xx/fallback=0`；`jitter(c16,mif32)` 下 Rust `p95` 优于 Dio，但 `p99` 仍偏高。

## 1.4) 会话补记（2026-03-03）

1. 已完成 Rust `DiskCache` 模块化重构（`engine/cache.rs` 拆分为 `engine/cache/` 子模块），行为口径保持不变，便于继续推进 P2 能力扩展。
2. 已回填测试记录 `TR-20260303-37`：`cargo fmt --check` + `cargo test -q` 通过（Rust `13/13`）。
3. P1 三个准入阻塞项状态未变化：upload 语义对齐、`maxInFlightTasks` 默认值统一、真机弱网/远端链路与归档规范闭环。
4. 结论保持不变：P1 仍处于“收尾与回填”阶段，暂不输出最终业务接入准入结论。

## 1.5) 会话补记（2026-03-03，upload 语义补齐计划）

1. 已完成差距确认：当前 Rust transfer 主流程未按 `kind` 区分 upload/download，upload 仍沿用下载写文件路径，和 Dio 侧“读取 `localPath` 作为上传源文件”的语义不一致。
2. 已确定补齐原则（以 Dio 为对齐基线）：`localPath` 在 upload 场景必须表示“本地源文件路径”；源文件不存在返回 `io`；仅 2xx 记为 completed；取消统一记为 canceled。
3. 已确定实现拆分顺序：
   - Rust 侧先补 upload 独立分支（文件流上传、进度/取消/状态码语义对齐）；
   - 再补跨通道一致性回归（Dio vs Rust 同口径用例）；
   - 最后补文档与测试记录回填。
4. 已锁定最小回归集（首批必须通过）：upload success、source file not found、non-2xx fail、cancel、fallback 安全边界（POST + `Idempotency-Key`）。
5. 下一会话默认从“先落 Rust upload 分支 + Rust 单测”开始，完成后再进入 Dart 侧一致性测试与 `test_run_log` 记录。

## 2) 当前正在做（In Progress）

1. 细化“接入业务 App 前”验收清单，补充 upload 一致性、配置生效性、弱网与远端链路三类证据。
2. 梳理 Rust transfer upload 的实现差距与回归用例范围（对齐 Dio 通道行为）。
3. 梳理 `maxInFlightTasks` 默认值统一方案（代码/CLI/文档同口径为 `32`）。
4. 规划 Rust 配置项生效性验证（`write_timeout_ms`、`max_connections`、`max_connections_per_host`）。
5. 推进真机 JSON 归档链路闭环（上传入口与登录/`token` 头已打通，待补齐命名约定与服务端回执口径）。
6. 推进远端链路 benchmark 联调（Go 服务契约文档已补齐，CLI 仍待开放 `--base-url` 透传以形成稳定自动化命令）。

## 3) 下一步准备做（Next）

1. 先补齐 Rust transfer upload 语义并新增跨通道一致性回归测试。
2. 统一 `maxInFlightTasks` 默认口径到 `32`（代码/CLI/文档），并重跑 `jitter` 基线。
3. 执行 Rust 配置项生效性验证（`write_timeout_ms`、`max_connections`），沉淀可复现结论。
4. 在 benchmark CLI 暴露 `--base-url` 并透传 `scenarioBaseUrl`，打通“真机 -> 公网服务”非 loopback 压测命令链路。
5. 基于 `http://47.110.52.208:7777/upload` 固化 JSON 命名约定与服务端回执口径，形成可追溯归档规范。
6. 落地 Wi-Fi/4G/弱网 + 远端链路补测并归档，回填 `docs/test_plans/test_run_log.md` 与策略文档，输出“业务 App 接入前”准入结论（继续 Rust 默认 / 临时回切 Dio）。

---

## 你现在只需要做的事（按顺序）

1. 先修 Rust transfer upload 语义，并补齐一致性回归测试。
2. 统一 `maxInFlightTasks` 默认值到 `32`（代码/CLI/文档），重跑 `jitter` 基线确认口径。
3. 在 CLI 暴露 `--base-url` 透传并联调远端 benchmark 服务，补齐非 loopback 压测入口。
4. 校验 `write_timeout_ms`、`max_connections` 等配置项是否实际生效并记录结论。
5. 基于已知上传入口固化命名约定与回执口径，执行真机弱网与远端链路补测并归档 JSON。
6. 最后回填 `test_run_log` 与策略文档，给出业务接入决策（Rust 默认或先回切 Dio）。

