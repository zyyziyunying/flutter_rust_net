# Repository Guidelines

## Scope
- This file covers `flutter_rust_net/example/` only.
- Read `../AGENTS.md` next for package-level rules.
- Workspace-level coordination rules live in `../../AGENTS.md`.
- Read `../../AGENTS.md` as needed when workspace-level coordination, ownership, or validation policy matters.
- This example app is a member of the repo-root Dart workspace; keep `resolution: workspace` aligned with the root `pubspec.yaml`.

## Reverse Discovery
- Use this file for example-local context.
- Use `../AGENTS.md` for `flutter_rust_net/` package rules.
- Use `../../AGENTS.md` for workspace-wide coordination, ownership, and validation policy when relevant to the task.
